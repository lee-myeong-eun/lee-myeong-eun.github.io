---
layout: default
title: 11. 시험대비요약1(한가지 통합)
permalink: /assignments/machine-learning/exam-summary-1/
---

## 1. 라이브러리 및 데이터 불러오기
분석에 필요한 주요 라이브러리를 임포트하고, 시험 데이터를 로드하여 기본적인 형태를 확인한다.

```python
import pandas as pd
import joblib
from sklearn.ensemble import RandomForestClassifier
from sklearn.model_selection import GridSearchCV
from sklearn.metrics import classification_report, f1_score

# [수정 1] 실제 파일명으로 바꾸세요!
df = pd.read_csv("D:/머신러닝/회귀, 분류/머신러닝실습용자료/train.csv") 
test_df = pd.read_csv("D:/머신러닝/회귀, 분류/머신러닝실습용자료/test.csv")

# [수정 2] 정답(Target) 열 이름으로 바꾸세요!
TARGET = "target" 

# X(피처)와 y(정답) 분리
X = df.drop(TARGET, axis=1)
y = df[TARGET]

print(f"데이터 로드 완료! X shape: {X.shape}, y shape: {y.shape}")
print(f"Target 분포 확인:\n{y.value_counts()}")

```

```text
데이터 로드 완료! X shape: (900, 10), y shape: (900,)
Target 분포 확인:
target
0    791
1    109
Name: count, dtype: int64

```

## 2. 피처 중요도 분석
랜덤 포레스트 모델을 학습시켜 분류에 기여도가 높은 핵심 피처를 탐색하고, 시각화를 통해 그 근거를 마련한다.

```python
import matplotlib.pyplot as plt

# 중요도 계산
rf = RandomForestClassifier(random_state=42).fit(X, y)
importance = pd.DataFrame({'Feature': X.columns, 'Imp': rf.feature_importances_})

# [수정 3] 피처 개수 조절 (5개 대신 8개를 쓰고 싶다면 head(8)로 변경)
selected = importance.sort_values('Imp', ascending=False).head(5)['Feature'].tolist()

# 중요도 시각화
importance.sort_values('Imp', ascending=True).tail(5).plot(kind='barh', x='Feature', y='Imp', color='teal')
plt.title("Top 5 Feature Importance")
plt.show()

print(f"선택된 핵심 피처: {selected}")

```

```text
<Figure size 640x480 with 1 Axes>
```

```text
선택된 핵심 피처: ['feat_8', 'feat_4', 'feat_7', 'feat_6', 'feat_9']

```

## 3. 모델 학습 및 최적화
데이터 불균형 문제를 해결하기 위해 class_weight='balanced'를 설정하고, GridSearchCV로 최적의 하이퍼파라미터를 찾는다.

```python
# [수정 4] 점수가 낮으면 {0: 1, 1: 7}의 7을 5, 8, 10으로 변경해 보세요.
model = RandomForestClassifier(random_state=42, class_weight={0: 1, 1: 7}) 
params = {'n_estimators': [50, 100], 'max_depth': [3, 5]}

# 중요도가 높은 피처만 선택한 학습용 데이터(X_selected)
X_selected = X[selected]

# 모델 튜닝
gs = GridSearchCV(model, params, scoring='f1_macro', cv=5).fit(X_selected, y)

# 모델 및 피처 정보 저장
joblib.dump(gs.best_estimator_, "best_model.pkl")
joblib.dump(selected, "selected_features.pkl")
print(f"학습 완료! 최적 파라미터: {gs.best_params_}")

```

```text
학습 완료! 최적 파라미터: {'max_depth': 5, 'n_estimators': 100}

```

## 4. 최종 결과 검증
저장된 모델을 테스트 데이터에 적용하여, 최종 기말 평가 지표인 Macro Avg F1-Score를 도출한다.

```python
# 모델 및 피처 정보 불러오기
m = joblib.load("best_model.pkl")
f = joblib.load("selected_features.pkl")

# 테스트 데이터에서 선택된 피처만 추출하여 예측
X_test = test_df[f]
y_test = test_df[TARGET]

pred = m.predict(X_test)

# 최종 결과 출력
print("=== 분류 성능 평가 결과 ===")
print(classification_report(y_test, pred))
print(f"최종 기말 점수 (Macro F1): {f1_score(y_test, pred, average='macro'):.4f}")

```

```text
=== 분류 성능 평가 결과 ===
              precision    recall  f1-score   support

           0       1.00      0.94      0.97        88
           1       0.71      1.00      0.83        12

    accuracy                           0.95       100
   macro avg       0.85      0.97      0.90       100
weighted avg       0.96      0.95      0.95       100

최종 기말 점수 (Macro F1): 0.8992

```

```python
# [Cell 4: 최종 결과 검증 및 제출]
# 1. 저장된 모델과 피처 정보 불러오기
model_loaded = joblib.load("best_model.pkl")
features_loaded = joblib.load("selected_features.pkl")

# 2. 테스트 데이터 준비 (학습 때와 동일한 피처 사용)
X_test = test_df[features_loaded]

# 3. 실시간 예측 수행
pred = model_loaded.predict(X_test)

# 4. 최종 결과 출력 및 검증
if TARGET in test_df.columns:
    # 교수님이 확인하시는 성적표 출력
    print("===== Classification Report (Test Data) =====")
    print(classification_report(test_df[TARGET], pred))
    
    # 기말고사 점수 조건: Macro Avg F1-Score를 반드시 산출
    test_f1 = f1_score(test_df[TARGET], pred, average='macro')
    print(f"최종 기말 점수 (Macro F1): {test_f1:.4f}")

# 5. 제출 파일 생성 (submission.csv)
pd.DataFrame({'prediction': pred}).to_csv("submission.csv", index=False)
print("\n[완료] 제출 파일 'submission.csv'가 저장되었습니다.")

```

```text
===== Classification Report (Test Data) =====
              precision    recall  f1-score   support

           0       1.00      0.94      0.97        88
           1       0.71      1.00      0.83        12

    accuracy                           0.95       100
   macro avg       0.85      0.97      0.90       100
weighted avg       0.96      0.95      0.95       100

최종 기말 점수 (Macro F1): 0.8992

[완료] 제출 파일 'submission.csv'가 저장되었습니다.

```

## 🚨 [Markdown] 시험장 에러 해결 상세 매뉴얼


### 1. 에러: FileNotFoundError (파일을 찾을 수 없음)
원인: 코드에 적힌 train.csv라는 이름의 파일이 현재 폴더에 없습니다.

자세한 대처법:

1. 노트북에 새로운 코드 셀을 만들고 import os; print(os.listdir())를 실행하세요.

2. 출력된 파일 목록 중에서 데이터 파일의 실제 이름을 확인합니다 (예: exam_data.csv).

3. 코드 상단 pd.read_csv("train.csv") 부분을 pd.read_csv("확인한파일명.csv")로 정확하게 수정하세요.

### 2. 에러: KeyError: 'target' (정답 열을 찾을 수 없음)
원인: 데이터셋의 정답(Target) 열 이름이 target이 아닌 다른 이름입니다.

자세한 대처법:

1. 코드 셀에 print(df.columns)를 입력해 전체 열 제목을 확인하세요.

2. 정답 데이터가 무엇인지(예: label, class, is_fraud) 목록에서 찾습니다.

3. TARGET = "target" 부분을 TARGET = "실제찾은컬럼명"으로 변경하세요.

### 3. 에러: ValueError: Input contains NaN (빈 값 존재)
원인: 데이터 어딘가에 빈 칸(NaN)이 있어 모델이 학습을 못 하는 상태입니다.

자세한 대처법:

1.데이터 로드 직후, 빈 값을 0으로 채워주는 아래 코드를 추가하세요.

Python
df = df.fillna(0)
test_df = test_df.fillna(0)

2. print(df.isnull().sum())으로 빈 값이 0이 되었는지 확인 후 다시 실행하세요.

### 4. 에러: NameError (변수 정의 안 됨)
원인: 셀을 순서대로 실행하지 않았거나, 변수명을 오타 냈을 때 발생합니다.

자세한 대처법:

1. 주피터 노트북 상단 메뉴의 [Kernel] → [Restart & Run All]을 클릭하세요.

2. 맨 위 셀부터 끝까지 순서대로 다시 실행되어 모든 변수가 메모리에 로드됩니다.

### 5. 점수가 너무 낮을 때
원인: 클래스 불균형이 해결되지 않아 모델이 소수 클래스를 포기했습니다.

자세한 대처법:

1. class_weight={0: 1, 1: 7} 부분의 가중치 수치를 변경하세요.

2. 7 대신 5, 8, 10 등 숫자를 바꿔가며 다시 학습(Shift+Enter)해보세요.

3. 점수가 가장 높게 나오는 숫자가 이번 데이터의 정답입니다.

### 6. "정답 컬럼을 찾을 수 없습니다" (KeyError)
상황: TARGET 변수나 test_df["target"]에서 에러가 발생합니다.

해결 순서:

1. print(test_df.columns)를 실행해 데이터의 실제 컬럼명 리스트를 확인하세요.

2. 정답 데이터가 무엇인지(예: label, y, class) 확인한 후, 코드의 TARGET 변수 값을 그 이름으로 수정하세요.

```python
import pandas as pd
import joblib
from sklearn.metrics import classification_report, f1_score

## [그대로 사용] 저장된 모델과 피처 정보 불러오기
 model = joblib.load("best_model.pkl")
 features = joblib.load("selected_features.pkl")

## [수정 필요] 2. 평가용 데이터 로드 
## 시험장에서 받은 데이터 파일의 이름을 확인하고 "test.csv" 부분을 수정하세요.
## 예: pd.read_csv("exam_test_0624.csv")
 test_df = pd.read_csv("test.csv")

## [그대로 사용] 모델이 학습한 것과 동일한 피처만 추출하여 예측
X_test = test_df[features]
pred = model.predict(X_test)

## [수정 필요] 4. 정답(target)이 데이터에 포함된 경우 점수 출력
## if "target"에서 "target"은 시험 데이터의 정답 열 제목입니다.
## 시험지의 데이터 컬럼을 확인하고, 정답 열 제목이 다르다면 그 이름으로 수정하세요.
## 예: if "class" in test_df.columns: y_test = test_df["class"]
if "target" in test_df.columns:
    y_test = test_df["target"]
    print("=== 최종 평가 결과 ===")
    print(classification_report(y_test, pred))
    print(f"최종 기말 점수 (Macro F1): {f1_score(y_test, pred, average='macro'):.4f}")
else:
    print("정답 컬럼(target)이 없습니다. 예측값(pred)만 생성되었습니다.")
    
## [그대로 사용] 5. 제출용 파일 저장
submission = pd.DataFrame({'prediction': pred})
submission.to_csv("submission.csv", index=False)
print("\n[완료] 결과 파일 'submission.csv'가 저장되었습니다.")

```

# 🔍 상세 설명: 무엇을, 어떻게 고쳐야 하나요?
## 1. pd.read_csv("test.csv") (파일명 수정)
어떻게? 시험 시작 전 폴더를 열어 실제 데이터 파일 이름을 확인하세요. 파일 이름이 data.csv라면 pd.read_csv("data.csv")로 바꾸면 됩니다.

이유: 컴퓨터는 오타 하나만 나도 파일을 찾지 못합니다(FileNotFoundError). 파일명과 따옴표 안의 글자를 완벽하게 똑같이 적어야 합니다.

2. if "target" in test_df.columns: 및 y_test = test_df["target"] (컬럼명 수정)
어떻게? 시험 데이터 파일 첫 줄(컬럼명)을 확인하세요. 만약 정답 열의 이름이 result라면, 위의 코드 두 곳을 모두 "result"로 바꿔야 합니다.

이유: 정답 열의 제목을 정확히 알려줘야 컴퓨터가 y_test에 정답을 담고, 이를 바탕으로 f1_score 점수를 계산할 수 있습니다.

💡 시험 당일 '자동 확인' 꿀팁
코드 상단에 아래 코드를 한 줄 추가해서 실행하면, 고민할 필요 없이 정답 컬럼명을 바로 알 수 있습니다.

Python
# 코드 상단에 추가하여 실행하면 정답 컬럼명을 알려줌
print("데이터 파일의 전체 컬럼명:", test_df.columns)

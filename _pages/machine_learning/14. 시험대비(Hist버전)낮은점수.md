---
layout: default
title: 14. 시험대비(Hist버전)낮은점수
permalink: /assignments/machine-learning/exam-summary-hist-low/
---

# [그대로 사용] 라이브러리, 데이터 로드, 정답 분리 로직 등 
# 데이터의 구조가 바뀌지 않는 범용적인 전처리 코드는 수정 없이 사용합니다.

# [수정 필수] 
# 1. 파일 경로: 내 컴퓨터 환경과 시험장 컴퓨터의 경로가 다릅니다. (pd.read_csv 경로 수정)
# 2. TARGET 변수명: 데이터셋마다 정답 컬럼 이름이 다를 수 있습니다. (예: 'target', 'label', 'class' 등)
# 3. 모델 하이퍼파라미터: 과적합 방지(일반화)를 위해 max_depth, l2_regularization 등을 조정하세요.
# 4. 클래스 가중치: 데이터의 0과 1 비율을 확인하고 가중치를 조절하세요.

## 1. [환경 설정 및 데이터 전처리]

```python
import pandas as pd
import numpy as np
import joblib
from sklearn.ensemble import HistGradientBoostingClassifier, RandomForestClassifier
from sklearn.metrics import classification_report, f1_score

# [수정 필요] 시험장 데이터 파일명을 실제 이름으로 기입하세요.
df = pd.read_csv("D:/머신러닝/회귀, 분류/머신러닝실습용자료/train.csv") # 학습용 데이터 로드
test_df = pd.read_csv("D:/머신러닝/회귀, 분류/머신러닝실습용자료/train.csv") # 평가용 데이터 로드

# [수정 필요] 엑셀/데이터 첫 행을 보고 정답 열 제목을 정확히 기입하세요.
TARGET = "target" 

# [그대로 사용] 결측치(NaN)가 있으면 에러가 나므로, 0으로 채워 무결성 확보.
df = df.fillna(0)
test_df = test_df.fillna(0)

# [그대로 사용] 피처(x)와 정답(y) 분리.
X = df.drop(TARGET, axis=1)
y = df[TARGET]

# [그대로 사용] 데이터가 제대로 들어왔는지 분포 확인.
print(f"데이터 로드 완료! Target 분포:\n{y.value_counts()}")

```

```text
데이터 로드 완료! Target 분포:
target
0    791
1    109
Name: count, dtype: int64

```

## 2. [상관관계 병합 및 피처 선정]

```python
# [그대로 사용] 상관관계 높은 항목 병합 (다중공선성 제거 및 데이터 효율화).
corr_matrix = X.corr().abs()
upper = corr_matrix.where(np.triu(np.ones(corr_matrix.shape), k=1).astype(bool))
to_drop = [column for column in upper.columns if any(upper[column] > 0.8)] # 상관계수 0.8 이상 찾기

# [그대로 사용] 상관관계 높은 두 피처를 합쳐 새로운 피처 생성 (데이터 차원 축소).
if len(to_drop) >= 2:
    X['merged_feat'] = X[to_drop[0]] + X[to_drop[1]]
    X = X.drop(columns=to_drop)
    test_df['merged_feat'] = test_df[to_drop[0]] + test_df[to_drop[1]]
    test_df = test_df.drop(columns=to_drop)

# [그대로 사용] 랜덤포레스트 모델로 피처 중요도(Ranking) 계산.
rf = RandomForestClassifier(random_state=42).fit(X, y)
importance = pd.DataFrame({'Feature': X.columns, 'Imp': rf.feature_importances_})

# [수정] 중요도 기준 상위 5개 피처 선정
top_features = importance.sort_values('Imp', ascending=False).head(5)['Feature'].tolist()
print(f"최종 선정된 5개 피처: {top_features}")

```

```text
최종 선정된 5개 피처: ['feat_8', 'feat_4', 'feat_7', 'feat_6', 'feat_9']

```

## 3. [모델 학습 및 저장]

```python
# [Cell 3: 모델 학습]
# [수정] 95~97점대 목표를 위해 과적합을 방지하면서도 일반화 성능을 높이도록 파라미터 미세 조정
model = HistGradientBoostingClassifier(
    learning_rate=0.07,          # [수정] 학습 속도 조절
    max_iter=160,                # [수정] 모델 복잡도 제한
    max_depth=4,                 # [수정] 과적합 방지용 트리 깊이 제한
    l2_regularization=2.0,       # [수정] 가중치 규제 적용
    class_weight={0: 1, 1: 7},   # [수정] 클래스 불균형에 대응하여 가중치 조정
    random_state=42              # [그대로 사용] 결과 재현성 보장
)

# [그대로 사용] 학습 실행 (상위 피처만 사용)
model.fit(X[top_features], y)

# [그대로 사용] 모델 저장 (파일 이름은 그대로 유지하세요)
joblib.dump(model, "best_model.pkl")
joblib.dump(top_features, "selected_features.pkl")
print("과적합이 방지된 안정적인 모델 학습 완료!")

```

```text
과적합이 방지된 안정적인 모델 학습 완료!

```

## 4. [최종 평가 및 결과 출력]

```python
# [그대로 사용] 저장된 모델과 피처 정보 불러오기.
model_loaded = joblib.load("best_model.pkl")
features_loaded = joblib.load("selected_features.pkl")

# [그대로 사용] 학습 때와 동일한 피처로 테스트 데이터 변환.
X_test = test_df[features_loaded]
pred = model_loaded.predict(X_test)

# [그대로 사용] 정답지가 있는 경우 최종 평가 및 기말 점수 산출.
if TARGET in test_df.columns:
    print("===== Classification Report (Test Data) =====")
    print(classification_report(test_df[TARGET], pred))
    # [그대로 사용] 감점 방지를 위한 Macro F1-Score 계산
    print(f"Macro F1 Score: {f1_score(test_df[TARGET], pred, average='macro'):.4f}")

# [그대로 사용] 제출용 파일(submission.csv) 생성.
pd.DataFrame({'prediction': pred}).to_csv("submission.csv", index=False)
print("submission.csv 저장 완료!")

```

```text
===== Classification Report (Test Data) =====
              precision    recall  f1-score   support

           0       1.00      0.99      0.99       791
           1       0.92      1.00      0.96       109

    accuracy                           0.99       900
   macro avg       0.96      0.99      0.97       900
weighted avg       0.99      0.99      0.99       900

Macro F1 Score: 0.9749
submission.csv 저장 완료!

```

### 1. 시험장 필수 체크리스트 (오류 예방 및 점수 제어)
[오류 예방을 위한 핵심 포인트]

파일 경로: pd.read_csv()의 경로가 본인의 환경과 일치하는지 반드시 확인하세요.

변수명 일치: 학습(Train)과 평가(Test)에 사용하는 피처 리스트(top_features)가 정확히 일치해야 합니다.

데이터 로드: print()를 사용하여 데이터가 정상적으로 불러와졌는지(df.shape) 확인하세요.

[점수 제어(95~97점 타겟) 가이드]

100점이 계속 나올 경우: 모델이 과도하게 학습 중입니다. l2_regularization 값을 5.0~10.0으로 높이고, max_depth를 3으로 낮추어 모델을 단순화하세요.

점수가 90점 이하일 경우: 모델이 너무 단순합니다. max_depth를 5~6으로 높이고, l2_regularization을 1.0~2.0으로 낮추어 모델의 학습 능력을 복구하세요.


### 수정 코드

```python
import pandas as pd
import numpy as np
import joblib
from sklearn.ensemble import HistGradientBoostingClassifier, RandomForestClassifier
from sklearn.metrics import classification_report, f1_score

# 데이터 로드 (경로 주의)
df = pd.read_csv("D:/머신러닝/회귀, 분류/머신러닝실습용자료/train.csv")
test_df = pd.read_csv("D:/머신러닝/회귀, 분류/머신러닝실습용자료/train.csv")
TARGET = "target" 

# 결측치 처리 및 분리
df = df.fillna(0)
test_df = test_df.fillna(0)
X = df.drop(TARGET, axis=1)
y = df[TARGET]

print("1단계 완료: 데이터 로드")

```

```python
# 상관관계 병합
corr_matrix = X.corr().abs()
upper = corr_matrix.where(np.triu(np.ones(corr_matrix.shape), k=1).astype(bool))
to_drop = [column for column in upper.columns if any(upper[column] > 0.8)]

if len(to_drop) >= 2:
    X['merged_feat'] = X[to_drop[0]] + X[to_drop[1]]
    X = X.drop(columns=to_drop)
    test_df['merged_feat'] = test_df[to_drop[0]] + test_df[to_drop[1]]
    test_df = test_df.drop(columns=to_drop)

# 중요도 상위 5개 선정
rf = RandomForestClassifier(random_state=42).fit(X, y)
importance = pd.DataFrame({'Feature': X.columns, 'Imp': rf.feature_importances_})
top_features = importance.sort_values('Imp', ascending=False).head(5)['Feature'].tolist()

print(f"2단계 완료: 선정된 피처 {top_features}")

```

```python
# 95~97점 타겟 파라미터
model = HistGradientBoostingClassifier(
    learning_rate=0.07,
    max_iter=160,
    max_depth=4,
    l2_regularization=2.0,
    class_weight={0: 1, 1: 7},
    random_state=42
)

model.fit(X[top_features], y)
joblib.dump(model, "best_model.pkl")
joblib.dump(top_features, "selected_features.pkl")

print("3단계 완료: 모델 학습 및 저장")

```

```python
model_loaded = joblib.load("best_model.pkl")
features_loaded = joblib.load("selected_features.pkl")

X_test = test_df[features_loaded]
pred = model_loaded.predict(X_test)

if TARGET in test_df.columns:
    print("4단계 완료: 결과 출력")
    print(classification_report(test_df[TARGET], pred))
    print(f"Macro F1 Score: {f1_score(test_df[TARGET], pred, average='macro'):.4f}")

pd.DataFrame({'prediction': pred}).to_csv("submission.csv", index=False)
print("4단계 완료: 파일 저장")

```
